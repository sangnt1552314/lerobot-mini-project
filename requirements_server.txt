fastapi
uvicorn
python-multipart

# MolmoAct2 inference. torch itself is NOT listed here - the PBS Singularity
# image (pytorch_2.6.0_cuda_12.8.sif) already provides a CUDA-matched torch;
# installing another one here could break GPU support.
transformers>=4.52.0
huggingface_hub
accelerate
pillow
numpy
